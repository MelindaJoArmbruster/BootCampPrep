// YOUR CODE BELOW
var price;
var rate;
var cost;
function taxCalculator(price, rate) {
    if (rate === 'NY') {
        return cost = price * 1.04;
}   if (rate === 'NJ') {
        return cost = price * 1.06625;
}
}